# Names only. Real values go in the hosting provider's settings or your local .env (never committed).
# Loaded by Pydantic settings; the bot refuses to start if a required value is missing
BOT_TOKEN=
DATABASE_URL=
REDIS_URL=
SENTRY_DSN=
LOG_LEVEL=INFO
