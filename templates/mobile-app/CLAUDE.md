# <PROJECT NAME> (Mobile app (Expo / React Native))

ASEN project. The `asen-engineering` plugin is enabled for this folder at project scope; its core rules load automatically at the start of every session.

## Identity (Rule 0)

- This folder is `ASEN\asen-<name>`, so git commits and pushes as `creativeasen`.
- Run gh as `GH_TOKEN=$(gh auth token --user creativeasen) gh ...`. Never switch the global gh account.
- Never touch client repos or folders that don't start with `asen-`.

## Skills to use

- `asen-engineering:mobile-app`
- `asen-engineering:security-core`
- `asen-engineering:supabase`
- `asen-engineering:dpdp-compliance`

## Commands

`/plan-feature` before building · `/audit` for a health check · `/prelaunch` before going live · `/incident` when something breaks · `/lesson` to save a lesson · `/handover` for docs.

## Project facts (fill in)

- Purpose and users:
- Personal data stored (see DPDP data map in `docs/PLAN.md`):
- Environments and URLs:
- Test command: · Typecheck: · Lint:
- Owner decisions still open:
